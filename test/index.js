import { getPosition, getCoords } from '../index.js';

const {
  QUnit: { module, test }
} = window;

module('getPosition', function (hooks) {
  hooks.beforeEach(function () {
    // todo
  });

  test('css animations', function (assert) {
    assert.expect(1);

    assert.true(true);
  });
});
